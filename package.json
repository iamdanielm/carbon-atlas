{
  "name": "carbon-atlas",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@cision/atlas-components": "github:cision/atlas-components#next",
    "carbon-web-components": "^1.21.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "set \"BUILD_PATH=./docs\" && set \"PUBLIC_URL=/carbon-atlas\" && react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
